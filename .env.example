# Postgres
POSTGRES_PASSWORD=cardapio_secret

# Backend (obrigatório)
JWT_SECRET=troque-por-uma-string-longa-e-aleatoria

# URLs de acesso público (ajuste para produção)
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3001
API_URL=http://localhost:3000

# SMTP para envio de e-mails (opcional — usa console.log como fallback)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
